# Arm AMBA spec PDF — redistributable text but kept out of git (see docs/spec/IHI0024E.md)
IHI0024E_amba_apb_architecture_spec.pdf
*.pdf

# SymbiYosys / Yosys proof output. Workdirs are directories under formal/ (named
# <sbybasename>[_<task>]); the only tracked files in formal/ are the sources. Ignore everything
# under formal/ except *.sv / *.svh / *.sby.
formal/*
!formal/*.sv
!formal/*.svh
!formal/*.sby

# node (wavedrom-cli via npx)
node_modules/

# editor / OS
.DS_Store

# tldr cache
.tldr/
