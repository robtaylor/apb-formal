# Arm AMBA spec PDF — redistributable text but kept out of git (see docs/spec/IHI0024E.md)
IHI0024E_amba_apb_architecture_spec.pdf
*.pdf

# SymbiYosys / Yosys proof output (workdirs: <sbybasename>_<task>)
formal/*_prove/
formal/*_cover/
formal/*_bmc/
formal/**/*.vcd
**/engine_*/
# sby task-status dir (named after the .sby basename)
formal/completer/
**/status.sqlite

# node (wavedrom-cli via npx)
node_modules/

# editor / OS
.DS_Store

# tldr cache
.tldr/
